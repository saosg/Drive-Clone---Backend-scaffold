apiVersion: v1
kind: Service
metadata:
  name: {{ include "drive-clone.fullname" . }}-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "drive-clone.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
