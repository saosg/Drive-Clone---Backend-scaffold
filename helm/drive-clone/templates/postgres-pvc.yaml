apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "drive-clone.fullname" . }}-postgres-pvc
spec:
  accessModes: [ "ReadWriteOnce" ]
  resources:
    requests:
      storage: 1Gi
